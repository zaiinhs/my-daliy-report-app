import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "My Daily Report App",
  description: " My Daily Report App adalah aplikasi yang membantu mencatat, mengelola, dan menganalisis laporan harian dengan efisien. Dilengkapi dengan fitur keamanan, pelacakan progres, dan integrasi yang memudahkan kerja tim. Tingkatkan produktivitas dan transparansi dalam pencatatan laporan harian Anda!",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
